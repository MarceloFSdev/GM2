# Local dev state from `wrangler pages dev` (local KV, caches) — never commit
.wrangler/
node_modules/.mf/
.dev.vars

# Local wrangler auth/session cache written by `npx wrangler` — not project state
node_modules/.cache/
